<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             x:Class="GavilanesExamen3.Views.BusquedaJGPage"
             Title="Johao Gavilanes">
    <StackLayout>
        <Entry x:Name="JG_entryBuscar" Placeholder="Ingrese el nombre del país" Text="{Binding NombrePais}" />
        <Button x:Name="JG_btnBuscar" Text="Buscar" Command="{Binding BuscarCommand}" />
        <Button x:Name="JG_btnLimpiar" Text="Limpiar" Command="{Binding LimpiarCommand}" />
    </StackLayout>
</ContentPage>